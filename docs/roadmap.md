# 待办清单

来自 2026-08-26 的一轮使用反馈。按"你能多快看见"排序，不是按难度。

勾掉的每一条后面跟着"怎么证明的"。README 自己写过一段：手工维护的状态行会说谎——它把
手机端、设置页和发布流水线写成"未做"，而三样都已经上线好一阵子了。所以这里只写有证据的。

## A — 界面

- [x] **A1 终端行的割裂感** — `lineHeight` 1.2 → 1.0。xterm 按格子刷背景色，任何行距都会在
      带底色的输出里留下一条横向缝。截图前后对比可见，`make render-check` 的行高断言钉住。
- [x] **A2 复制就是复制** — 选中即写剪贴板（`pointerup` 上触发）。浏览器拒绝写入时退回原来
      那颗按钮，不是静默失败。
- [x] **A3 粘贴图片** — 捕获阶段的 `paste` 监听拿到文件 → 走上传通道 → 绝对路径填进命令行。
      render-check 里构造真的 `ClipboardEvent` + `DataTransfer` 验证（Playwright 的
      `dispatchEvent` 不带 clipboardData，一开始把一个好功能测成了坏的）。
- [x] **A4 右栏空间利用率** — 四个 tab 改成等宽 segmented control，头部 h-8 → h-10；CPU/内存/
      磁盘常驻底部一条 `SystemStrip`，不再要切到监控 tab 才看得到。
- [x] **A5 目录选择和新建目录** — `DirectoryPicker`：面包屑、上一级、新建即进入，外加一个
      手输路径的口子（HOME 之外的地方）。后端 `browse.Dirs` / `browse.Mkdir`，名字里带
      `/` 或 `..` 一律拒绝——把这条守卫拿掉，`../escape` 就穿出去了，测试会红。
- [x] **A6 icon** — `icon.svg` + 180/192/512 PNG + manifest。标签页、主屏图标、README 头图同一张。
- [x] **A7 整体太丑、太缩** —— 做完的部分：三层 surface + elevated 令牌（亮色不再是纯白、
      暗色不再是纯黑），终端嵌进圆角边框里，右栏 tab 改等宽 segmented control 加常驻系统条，
      **把九种字号收成五级、六种圆角收成四种**（九种不是一套刻度，是在九个不同时刻做的九个
      决定——11px 和 11.5px 单看毫无差别，八个这样的差别加起来就叫「不精致」；收拢时一律
      往大走，因为最初的抱怨就是太挤），弹窗有了入场动效和按压反馈，目录选择器全键盘可用。
      `scale.test.ts` 钉住：组件里出现任意 `text-[13px]`、或者 Tailwind 自带的 `text-sm`，
      都会红。**剩下的是口味**，那部分等你看了具体指哪儿。

## B — 语言

- [x] **B1 简体中文** — `web/src/i18n.ts`，`useSyncExternalStore` + localStorage，跟随
      `navigator.languages`，设置页可切。两种语言写在同一行上，漏翻一眼就看得见；
      `i18n.test.ts` 断言两边都没有留下未替换的 `{占位符}`。
- [x] **B2 "状态靠猜"的文案** — 重写成中文，短句，不再像在怪人。

## C — 平台能力

- [x] **C1 PWA** — manifest + service worker。SW **故意不缓存任何东西**：一个会缓存的 SW 会
      把面板钉在旧版本上，而这个项目的整个前提是后端可以随时重启升级。
- [x] **C2 通知权限 + 手机推送** — 只在"进入 waiting 的那一次跳变"上推，第一份快照静默播种，
      页面在前台时不推。
- [x] **C3 每个会话的 CPU / 内存** — `GET /api/usage`：一次遍历 `/proc`，按 pane pid 把整棵进程树
      的占用汇总到会话上。百分比用**整机**做分母（和上面的机器仪表同一套），不用 top 的
      「100% = 一核」——两个数字挨在一起时，会话 310% 配机器 31% 只会让人得出唯一一个错误结论。
      pane 已经没了的会话是**缺席**而不是 0（0 是一个真实读数）。render-check 端到端钉住，
      把归属逻辑拿掉会红。
- [x] **C4 开放 API + API 文档** — API 令牌（存 SHA-256，只显示一次，可单独吊销）+
      `docs/api.md`。文档和路由表**双向**校验：漏写的接口和多写的段落都会让构建红。

## D — 部署

- [x] **D1 systemd 高优先级、防 OOM** — 实测：user unit 写 `OOMScoreAdjust=-500` 进程读到
      `100`，system unit 带 `User=` 才真的是 `-500`，而 `systemd-analyze verify` 两种都放行。
      于是多了一份 `deploy/vibepanel-system.service`，把这段测量写在文件头上。
      两份 unit 都加了 `CPUWeight=200` / `IOWeight=200` / `ManagedOOMPreference=avoid`。
- [x] **D2 开机自启 + 安装脚本** — `install.sh` 现在会替你 `enable-linger`（不需要 root），
      并且单元已经在跑时是 `restart` 而不是 `start`，还会告诉你发生了哪一种。
- [x] **D3 也能当普通进程跑** — `./vibepanel serve` 一直就行；Docker 也有，但镜像头部写清楚了
      在容器里重启面板会杀掉所有会话。

## E — 发布

- [x] **E1 专业易懂的 README** — `README.md`（英文）+ `README.zh-CN.md`（简体中文），带真实截图，
      截图由 `web/scripts/shots.mjs` 从真的面板拍出来，不是画的。
- [x] **E2 推到 GitHub public** — https://github.com/jiangmuran/vibepanel ，CI 在
      `.github/workflows/check.yml`，已经绿了。CI 从第一次跑开始一共抓到六个真问题，
      全都是「在我这台机器上看不见」的那一类：tmux 3.4 不认 `allow-set-title`；tmux 3.5
      以下会把 `-F` 输出里的 0x1F 分隔符转义成 `\037`（而且不转义反斜杠，所以没法还原）；
      旧 tmux 的整屏重绘里全是换行，被当成了「屏幕前进」从而清掉了等待中的铃；
      `Inspect` 的提前返回把 `events` 发成了 `null`；一个测试的铃和 attach 在抢跑；
      还有一个测试没有像面板那样设 `TERM`。

## F — 热升级

- [x] **F1 版本漂移要说话** — 重连后比对 `/api/health` 的 `version@commit`，变了就出横幅请你刷新。
      `restart-check` 里真的构建第二个二进制（`-X version.Version=v0.0.0-upgrade-check`）来验证，
      并且确认 `BEFORE_THE_UPGRADE` 的输出在升级后还在。把检测拿掉，这条检查会红。

---

## 记账规则

和 `docs/build-log.md` 一样：改完要能证明它改对了。界面的东西用 `make render-check` 驱动真浏览器，
逻辑的东西用测试，并且**把修复挪走确认测试会红**。

---

## 路上记下的

- **`TestDetachAllDoesNotTakeTwoSecondsPerSession` 偶发失败。** 在一次同时开着真面板（3 个会话
  在被轮询）并夹着重启的 `make check` 里红了一次，之后单跑和连跑三次全绿，失败信息没抓到。
  它的断言是"超过 8 秒才算失败"而整个测试只花 2.03 秒，所以红的多半是里面的 `Create`/`Attach`
  在竞争下 `Fatalf`，不是那条计时断言。未复现，未修，先记着。
- **亮色终端的 bright 行低于 AA。** 白底上的 bright green / bright yellow 对比度不到 4.5:1。
  这是终端配色的常规取舍（要和暗色保持同一套色相），记在这里当作一个明确的决定，而不是没看见。

---

## 第二批反馈（2026-08-27，使用中陆续提的）

**这一节是权威清单。** 上下文会被压缩，聊天记录会滚走，这个文件不会。每一条要么勾掉并
写上「怎么证明的」，要么留着。全部勾完之前不算做完。

### 终端本身

- [x] **T1 终端渲染有裂缝**（clawd 玩偶、方块画、进度条）——`@xterm/addon-webgl`
      一直在 `package.json` 里，但**从来没有被 `loadAddon` 过**，所以每个会话都跑在 xterm 的
      DOM renderer 上：一个 cell 一个 span、各自画背景、按 CSS 像素定位；cell 尺寸不是整数
      像素时它们不严丝合缝，缝隙透出背景 = 裂缝。加上 `customGlyphs`（方框字符按几何画）和
      `rescaleOverlappingGlyphs`。**截图已确认**：圆角框、实心进度条、渐变块全部无缝。
      顺带加了 `设置 → 渲染器` 的 DOM 回退开关，给 WebGL 有问题的机器留一条路。
- [x] **T2 上下滚不动** —— attach 时用 `capture-pane -S - -E -1` 给回放环预热。这段代码
      曾经有过、又被正确地删掉了（当时 tmux attach 以 `ESC[?1049h` 开头，之后一切都画在
      xterm 的 alternate screen 上，那里按定义没有 scrollback）；而删除时留下的注释点名了
      能改变这一点的设置，`terminal-overrides ',*:smcup@:rmcup@'`，那条设置后来正是为此加上了。
      挪掉预热，回放里就只剩 200 行历史里的 HIST_178..200——正是你看到的现象。
      `restart-check` 端到端钉住：重启后仍能滚回重启前一百行的第一行。
- [x] **T3 全屏（alt-screen）模式** —— 浏览器看不出 TUI 在跑：tmux 按 pane 模拟 alternate
      screen 并把它合成掉，而面板又故意让 tmux 自己的 client 不进 alternate screen（否则根本
      没有 scrollback）。所以改由 poller 告诉它——`#{alternate_on}`，`internal/tmux` 一直在读、
      从来没人用过。全屏程序在画的时候不提供滚动条，视图贴底。用「贴回去」而不是「冻住」：
      一个什么都不做的滚轮事件读起来像页面卡死，而且应用自己想要滚轮（这就是触摸板在 TUI
      里本来就正常的原因）。
- [x] **T15 滚动条挡字** —— **有两条滚动条**。xterm 6 把滚动挪到了
      `.xterm-scrollable-element`（div 做的覆盖式滚动条），而 `.xterm-viewport` 变成了一个
      没有子节点的背景层（实测 830×602、`kids: []`），xterm.css 却仍然给它
      `overflow-y: scroll`——于是画出第二条永久存在、完全无用的轨道。它占多少宽度是**平台
      决定**的：经典滚动条的机器上占 8–15px 且 fit addon 会减掉；覆盖式滚动条的机器上占 0，
      fit addon 于是多排几列，真正那条 14px 的滚动条就压在最后一列字上——这就是它在有的机器
      上挡字、有的不挡的原因。现在把死的那条藏掉，用 padding 确定性地留出宽度。
      另外两条给 `.xterm-viewport::-webkit-scrollbar` 的样式自 xterm 6 升级起就没作用过。
- [x] **T4 粘贴图片** —— 终端本来就在监听 paste，但只监听在它自己的 host 元素上，而 paste
      事件是送给「谁拿着键盘」的：焦点在侧栏一行、面板 tab、或者干脆什么都没选中的时候
      （也就是几乎每次点击之后），事件根本到不了终端。现在在 `document` 上捕获阶段监听，
      **只处理带文件的 paste**（纯文本 paste 是别人往输入框里粘东西，一律不碰）。
- [x] **T14 切 tab 后焦点回终端** —— 规则写在 `focus.ts` 里：**只有当人主动选了一个终端
      （侧栏会话、底部 tab、右栏 tab），并且在那个终端真正准备好接收焦点的那一刻没有别的东西
      占着键盘时**，焦点才移动。从点击处理器调用而不是从 effect：会话死掉时 `applyState` 会
      改选第一个会话，用 effect 会因为「别处一个构建结束了」把焦点从笔记框里抢走。

### 会话与恢复

- [x] **T5 关机/重启后恢复** —— 迁移 v9 记下真正能重跑的 argv（`sessions.command` 看着像
      答案其实不是：它存的是 `#{pane_current_command}`，poller 每两秒覆盖一次），每 30 秒把
      每个会话最后 2000 行 / 256 KiB 回滚存档，重启后**列出来问你要不要恢复**而不是默默拉起
      两打 agent。重建的是会话不是进程——agent 的上下文回不来，这一点在 pane 里用中英双语横幅
      和 header 上的「已恢复」标记说了两遍。实测：整机 6 个会话满历史存档 40.7ms，
      没输出时一轮 219µs；24 个会话全部顶到上限时数据库 6.2MB。
- [x] **T10 Codex** —— 一键安装 `notify`（按行改 TOML，插在第一个 table 之前；追加到末尾会
      变成 `[某表].notify`，能解析、能过 `codex doctor`、读回来也对，但 Codex 永远不读）。
      标题链路的断点：程序发现自己在 tmux 里时用 passthrough 发 OSC，passthrough 的定义就是
      tmux 不看，所以 `pane_title` 永不改变；而面板**本来就在解析那个标题**、送到浏览器、
      然后没人接。现在 `Live` 记住 PTY 上看到的标题，`deriveTitle` 把它作为第二来源。

- [x] **T5 关机/重启后 100% 恢复进度。** 迁移 v9 记下了真正能重跑的 argv（`launch_command`——
      原来的 `command` 是 `#{pane_current_command}`，是个标签不是命令行）；`session_scrollback`
      每 30 秒存一份有界的 `capture-pane`（2000 行 / 256 KiB），关机时再存一次，所以正常
      `reboot` 一行不丢，断电最多丢半分钟。恢复是**明确的**：面板列出每个会话会跑什么命令、
      在哪个目录，一键或逐个恢复，另有 per-session 的开机自动恢复开关（默认关——开机同时
      拉起二十几个 agent 比现在这个问题更糟）。回滚记录会被打到新 pane 的 history 里，
      上面压一条中英双语横幅说明「以上属于一个已经不存在的进程」，UI 里另有 `restored` 标记。
      **恢复不了的是进程本身**——agent 的上下文随进程一起没了，重跑命令启动的是一个全新的
      agent。文档、API 和界面都不许假装不是这样。

### 界面质感

- [x] **T6 弹窗质感与动画** —— 背景淡入 + 模糊，面板从 0.97 和下方 10px 进场（和别处同一条
      曲线），控件按下 3% 缩放；确认框和输入框都换成自制的（见 T11）。目录选择器补上了每个
      文件对话框三十年来都有的四个键。已截图确认。顺带修了 `shots.mjs`：它用三个猜测去找
      「新建项目」按钮（一个不存在的 testid、一个被翻译过的 title、一段英文按钮文字），
      于是一直悄悄什么都没拍到，只打印一行没人看的提示。
- [x] **T11 不用 raw toast** —— 五处 `window.confirm` / `window.prompt` 全部换掉：自制
      toast 栈（形状+颜色双重编码、自动消失、可手动关、手机上锚在软键盘上方的输入条之上）
      和确认对话框（Escape 取消、Enter 确认、焦点默认落在安全那一侧、危险按钮是窄的那个）。
      `no-raw-dialogs.test.ts` 同时防住 `window.confirm(` 和裸的 `confirm(`。
- [x] **T12 目录选择能搜索** —— 一个框，两件事，由你输的内容决定：以 `/` 或 `~` 开头就是
      路径（回车跳过去），否则就是筛选当前目录。手输路径这件事本来就有，但那个框在**底部**、
      在列表和「新建目录」下面——一个没人看得见已经被回答了的答案。现在框在顶部，自动聚焦，
      方向键不用离开输入框就能选列表。
- [x] **T19 目录选择器重做**（「那个控件我认为仍然得完全重构」）—— 不是重新上色：一个列表、
      一个贴在上面的筛选框、一行「新建目录」、两个按钮，五个各自正确、从来没有一起被决定过的
      零件，内容排在眼睛的第四位。现在是**一个列表加一个框**，而那个框是一个控件干了原来三个
      的活：面包屑就长在框里、在光标左边，点某一段就去那里；打字就筛选；打的东西以 `/` 或 `~`
      开头，面包屑就让开——你已经不是在搜一个地方，而是在指一个地方。标题栏删了（`aria-label`
      留着）。按钮上写清楚回车会干哪一件事（`进入` / `用这个路径`），因为原来那个「使用这个
      目录」在框里有路径时会**悄悄**用输入的那条。往下是从右边进来、往上是从左边进来（
      `prefers-reduced-motion` 下两者都没有，所以面包屑任何时候都写着你在哪）。拒绝的话贴在
      被拒绝的那个控件下面，不再是按钮上方那一条什么都往里塞的横幅。读取中是四条骨架行加一句
      话，空目录和「筛不出东西」是两句不同的话、各自给出下一步。键盘只剩一条规则：**框里一有
      字，编辑文字的键就归框**（空框时 Backspace 上一层；筛选时 Home/End 归列表，因为筛选词
      只有五个字符而目录有两百行）。判断逻辑全部搬到 `web/src/components/dirpicker.ts`，36 个
      测试，18 个变异逐个验过会挂；另有两个测试盯住浏览器检查用的 `data-testid` 和动效 class
      与 `styles.css` 的对应——那两处一旦漂移，代价是二十分钟后一次看起来像功能坏了的失败。
- [x] **T13 文件 tab 上传与预览** —— 拖进去、粘贴、按钮选文件三条路；预览走
      `GET /api/projects/{id}/preview`，**按内容嗅探类型而不是按扩展名**（SVG 当成文本，因为
      它是能跑脚本的文档）。三个上限而不是一个：传输 8 MiB（图片/PDF 超了直接 413，半张图什么
      也画不出来）、文本 256 KiB（截断而不是拒绝，切在整行上）、4000 行（256 KiB 的 `a\n` 在
      280px 一栏里是二十五万个行盒——字节预算管传输，不管排版）。前后端用同一个数字，有测试钉住。
      顺带发现一处真漂移：终端的 paste 读 `clipboardData.items`（Chromium 上**粘贴**的截图只在
      那里），App 的 drop 读 `dataTransfer.files`（**拖进来**的文件只在那里），两边互相看不见
      对方的情况。

### 数据

- [x] **T18 token 统计** —— 数字只来自 agent **自己写的 transcript**，没有任何估算器
      （按字符数除以四是一个看起来像测量的东西，而它不是）。三件必须**验证而不是假设**的格式
      事实，每一件都会改变答案：Claude 的 transcript **每个 content block 一行、都带同一个
      usage 对象**——一份真实的 89MB 记录里 13,869 行 usage 对应 6,563 个请求，**多算 2.37 倍**；
      重复分两种形态，57,296 个是相邻的，**466 个隔了整整 1,787 行**（会话恢复时把整段历史重放
      进同一个文件），所以 ingest 游标必须以整个文件为单位而不是字节偏移，滑动窗口能过第一种、
      会静默重复计第二种；Codex 的 `input_tokens` **包含**缓存部分而 Claude 的不包含，不归一化的话
      这台机器上最大的 Codex 线程会显示 5240 万「新输入」，其中 5070 万其实是缓存读取。
      实测：568 份 transcript、2.16GB，冷启 3.09s，没变化时 35ms。28 条变异全红，其中**两条
      抓到了真 bug**（`eachLine` 的 16MiB 上限只在缓冲区填充时检查、最后一块从不检查；还有一个
      测试其实在钉标准库而不是这个包）。

### 部署与运维

- [x] **T7 交互式安装脚本** —— 有 TTY 就问、`--yes` 保持无人值守；检测到 root 就提供系统
      服务安装（systemd + 开机自启），没有就说明原因退回 user unit；**绝不同时装两份**。
      新增 `make install-check`（72 条断言、几秒、不用 sudo），每条守卫都挪掉验证过会红。
- [x] **T16 安装方式对比** —— 两份 README 的「安装」开头各加了一张表：user service / 系统
      服务 / 直接跑 / Docker，四行分别写清楚**什么时候用、会话能活过什么、需不需要 root、
      开机是否自启**。Docker 那一行说的是实话：容器里什么都活不下来。
- [x] **T8 面板内更新** —— 仓库是**编译进去的常量**而不是设置项：可配置的更新源等于一次数据库
      写入就能让面板装上别人选的二进制。要装哪个版本也不是参数——请求说不出口，只能装最新的
      release，否则 409。校验和买到的东西写在文档里而不是暗示：它能发现下载损坏或截断，仅此而已，
      因为 sums 和归档来自同一个 release。三条守卫各自被挪掉验证过会红：开发版构建不能说服自己
      去更新（`dev` 读不懂，把读不懂当成「落后」会每次都提示更新）；用 rename 而不是覆写
      （运行中的二进制不能原地重写，但可以被 rename 覆盖，旧的留在 `.old`）；先解析符号链接
      （否则 `~/.local/bin/vibepanel` 会从链接变成文件，真正的二进制成了孤儿）。
- [x] **T9 只读分享链接** —— 独立的 `share_links` 表（迁移 v10）而不是 `api_tokens` 上加一个
      `scope` 列，这就是整个安全设计：**flag 是每个 handler 都要记得读的东西，而忘记读的那个
      handler 是明年才写的那个**。两张表意味着「这个凭据能不能碰终端」由查表查的是哪张表回答，
      而这个查询只写了一次——一条路由的中间件里。链接从不泄露路径、cwd、命令行、tmux 名、
      主机名和真实 id；行 id 是 `HMAC(token hash, 真实 id)`，同一个链接稳定、不同链接不同，
      所以两块屏幕对不上号。连接状态五种（连接中/在线/重连中/已断开/已失效），每种有自己的
      **形状**加大字，外加「这些数字是什么时候的」——由**独立的一秒计时器**驱动，跟轮询共用时钟
      的话它会和数字在同一刻一起冻住。三条安全变异我在这边跑过：拿掉中间件、跳过 allowlist、
      明文存 token，全部会红。

### 路上产生的欠账

- [x] **T17 浏览器检查改读 buffer** —— `window.vibepanelScreen` + `scripts/lib/screen.mjs`，
      四个检查脚本的九个调用点全部改完；需要 DOM 几何的两处（CJK 进格宽度、触摸拖动的行位置）
      钉在 DOM renderer 上，这也正是设置里那个渲染器开关的另一个用处。render / stress /
      restart 三项全绿。

---

---

## 第三批：核对 session 记录后发现**漏掉**的（2026-08-27）

这一节是把整个会话记录里每一条 prompt 抽出来逐条核对之后补上的。漏的来源是一条我从来
没有记进任何清单的消息——一份十点功能列表，当时被当成「现状确认」读过去了，而它其实是
需求。这就是这份文件存在的理由：**没有写进来的东西，压缩一次就没了。**

- [x] **U1 opencode 的 hook 自动注入** —— 三个 agent 里最干净的一个：opencode
      **自动发现** `~/.config/opencode/plugin/` 下的每个 `*.js`/`*.ts`，不需要改任何配置文件。
      这是实测出来的，不是猜的——往那个目录丢一个文件，`opencode debug config` 会把它报成
      `plugin_origins: [{ scope: "global" }]`。所以安装就是写一个本来不存在的文件，卸载就是删掉它，
      而不像 claude 要合并 JSON、codex 要往 TOML 第一个 table 之前插一行。
      插件映射：`chat.message` / `tool.execute.before` → working，`permission.ask` → waiting，
      `session.idle` → done。没有那三个环境变量时**返回空 hooks**，所以从普通终端起的 opencode
      跑的不是「我们的东西不生效」，而是根本没有我们的东西。
      两条守卫都挪掉验证过会红：**按字节比对**（旧版本留下的插件是「装了但是错的」，
      而「文件存在」正是这个项目一再发现会骗人的检查）；以及**安装时不留备份**——那个目录里
      opencode 会加载每一个 `.js`，`vibepanel.js.bak` 等于装了第二份、每个状态报两次。
- [x] **U2 GitHub 预览。** 本地那半永远可用、不联网、不要凭证：分支、领先/落后、未提交（分开数
      staged/unstaged/untracked/**冲突**——合在一起冲突就消失了）、最近提交，以及**只列出和项目根
      不在同一个 commit 上的会话**——六个 agent 在一个目录里干活时，别的屏幕都给不了这一条。
      联网那半（PR + CI）**按一下才走**，没有 token 也照常工作。结构性守卫：`internal/git` 是
      **默认拒绝的子命令白名单**，加上 `GIT_OPTIONAL_LOCKS=0`——没有它面板会在 agent 执行
      `git commit` 的时候拿走 `.git/index.lock`，**失败的是 agent**。原本的说明保留在下面：
      文件预览做了（文本/图片/PDF），
      GitHub 那半没有。
- [x] ~~**U3 内置 VNC。**~~ **整体撤回了，见 X2。**下面留作记录：
      面板是 **RFB 代理**，不是 VNC 客户端：浏览器走 WebSocket，
      VNC server 走 TCP，字节只在 `internal/httpapi/vnc.go` 一个地方过。握手两头都由面板
      终结——它拿存下来的密码去跟 display 认证，然后对浏览器只 offer RFB 3.8 + security
      type `None`，所以那 8 个字节从来没进过浏览器。ClientInit 之后原样对拷，编码
      （Tight / ZRLE / JPEG）由两端自己谈，面板一个像素都不解。
      **地址永远不在请求里**：浏览器给的是一行的 id，host/port 从库里的行读。行本身再受
      `--vnc-allow` 约束，**空表示只允许回环**（跟 `--allow-from` 相反，故意的），存的时候查
      一次、每次连接再查一次，而且要求域名解析出来的**每一个**地址都在策略内，然后按 IP
      字面量去 dial——中间没有第二次解析可以给 rebinding 用。
      「只看」在代理里落实（丢掉键盘、指针、剪贴板、resize、xvp），不是在 noVNC 上设个属性。
      客户端用 noVNC（MPL-2.0，未修改，动态 import 单独成 chunk）；自己写一个的话，
      难的从来不是画布，是 Tight/ZRLE 和键盘映射。
      「卡住」和「没动静」用**探针**区分：静默 5 秒发一条 non-incremental 的 1×1
      FramebufferUpdateRequest（server 必须回），再 5 秒没回就是 not responding，形状和文字
      都跟 live 不一样。
- [x] **U4 HTML 预览。** 四层隔离，真正起作用的是第二层:**`sandbox` 不带 `allow-same-origin`**,
      被框住的页面拿到的是不透明 origin,够不到会话 cookie。响应上的 `default-src 'none'` 是
      「不联网」还成立的原因——别人页面里一个远程 `<img>`、一个 webfont 或一个嵌套 iframe,
      否则就是面板自己那个标签页发出去的、没人要求过的外连;它还带着 CSP 的 `sandbox` 指令,
      这是 iframe 属性做不到的(有人把 URL 直接敲进标签页的情况)。脚本默认关,而且**这个决定
      在服务端**,所以在 devtools 里改属性一无所获。`blob:` 和 `srcdoc` 都被否掉,理由比
      「origin 继承」更硬:**它们没有响应头**,于是承载 CSP 的那一层直接没了。原说明保留:
      现在的预览把 HTML/SVG **当作文本**显示——那是故意的（SVG 是能跑脚本
      的文档，用 `<iframe>` 会在面板自己的源上执行它）。但「预览一个 html 页面」这件事没做，
      要做就得想清楚怎么隔离。
- [x] **U5 按启动参数 / apihost 的快速配置。** 具名启动配置：名字 + argv + 环境变量，
      迁移 v13。**没有做 apiHost 字段**——哪个变量装 endpoint 是各家 agent 自己定的
      （claude 是 `ANTHROPIC_BASE_URL`，codex 是 `OPENAI_BASE_URL`，opencode 一个都没有，
      它按 provider 在自己的配置里选），面板去猜这个映射，猜错的那天就是「设了一个没人读的
      变量」，而会话看起来是配好的。所以配置里装的是变量，内置目录里预填变量名。
      密钥可以存：不存的话人就会去 argv 里写 `env KEY=... claude`，那才是最糟的——落进
      `launch_command`（恢复对话框会打印在屏幕上）、落进 `ps -eo args`（同机器任何用户可读，
      实测），还每次恢复都重跑一遍。存了之后 `secret` 的值**再也不会发回浏览器**、只走
      tmux 的 `-e`、不进审计日志；但它在 SQLite 里是明文，设置页面用一行字说明白了。
      顺序是安全边界：实测 tmux 3.6 两个 `-e` 同名取**最后一个**，所以面板自己的
      `VIBEPANEL_*` 排在配置的后面，加上保存时拒绝该前缀，两道。
- [x] **U6 webhook 通知（微信 / 安卓 / iOS）+ 群猫娘。** 一套机制，不是一串服务名：
      `{method,url,headers,body}` 加上 `{state}{session}{project}{url}{time}` 替换，就是 Bark、
      ntfy、Gotify、Server酱、PushPlus、Slack、Discord 和反代后面的一个 shell 脚本。
      **两种转义**，按占位符在哪里选：URL 里走百分号编码（`fix a&b` 不这么做会在 `&` 处
      断成另一个查询参数），body 里走 JSON 转义（agent 标题里全是引号）。触发的是**状态
      转换**而不是状态，所以一个一直等着的会话不会每两秒发一条。
- [~] **U7 插件系统 —— 设计完了，结论是不要做运行时。** 原话是「正在设计的插件和 api 系统
      可以让一个 harness 接入管理所有 session」。逐条拆下来，「插件」是四件事共用一个词：
      **响应 session 变化**（API 令牌 + `/ws` 推送已经做到，而且比进程内插件好）、
      **把事件发到别处**（webhook 已经做到）、**往界面里加东西**（拒绝：面板这个 origin 上
      挂着可写终端和会话 cookie，第三方 JS 就是「以你的身份跑任意代码」）、
      **打包成一个文件而不是一个守护进程**（真正缺的那件事，但那是分发问题不是运行时问题）。
      `docs/plugins.md` 是完整论证：能力模型、装一个插件意味着什么（照实说：以运行面板的
      用户身份跑任意代码）、慢/抛异常/死循环怎么办（**不能让 session 状态停止更新**，
      `fireWebhooks` 那道疤就是这条规则的来源）、以及怎么做版本。
      唯一动的代码是 `docs/api.md` 里新加的「Attaching a harness」一节——把三样本来互不相识
      的东西写在一起：那就是插件系统，它只是一直没有名字。

### 一处需要你拍板的偏离

原话第 8 条是「整个项目采用 go 编写，**单文件省资源无需依赖不联网**」。前三样都成立
（`CGO_ENABLED=0`、单二进制、内嵌前端）。但 **T8「面板内更新」会联网**——只在你按下按钮时
问 GitHub 要一次 release，没有心跳、没有遥测、不会自己检查。如果「不联网」是硬约束，
这个功能应该做成默认关闭或者干脆去掉；这是你的决定，不是我的。

### 已经做到的（同一条消息里的其余几点，逐条对过）

1 网页端 + 各类 auth ✅（密码 / passkey / API 令牌 / `--allow-from` / TLS）·
2 按项目和标题分组自动排序 ✅ · 4 tmux 跑在守护进程、与前端无关、最高优先级防 OOM ✅ ·
5 的其余部分：待办 ✅ 快速终端 ✅ 文件 ✅ 资源监视器 ✅ ·
8 go / 单文件 / 无 cgo 依赖 ✅ · 10 的 API 一半 ✅ · PWA + 系统通知权限 ✅

### 更早那条也核对过（Apple 风格、笔记待办、手机端、软键盘、passkey、滑动复制）

Apple 风格设计语言 ✅ · 笔记与待办可分屏可切换 ✅ · 手机端独立外壳 ✅ · 自制软键盘条 ✅ ·
Passkey + 密码 ✅ · 滑动复制 ✅

---

## 第四批（2026-08-27 晚，UI/UX 与分发）

- [x] **V1 目录选择器完全重构。** 一个输入框既是过滤器也是地址栏：面包屑就在光标左边，
      输入绝对路径时它们把宽度让出去。进入下一层从右边来、返回上一层从左边来——和每部手机
      的导航栈是同一个约定，不用教。慢的那次读盘给的是四条骨架而不是转圈。
- [x] **V2 面板 chrome：动效与统一。** `.vp-control` / `.vp-tab` / `.vp-chrome` / `.vp-divider`
      / `.vp-grip` / `.vp-segmented` 各只有一份定义，高度是 `height` 不是 padding——padding
      正是「16px 图标和 13px 图标在同一排里变成两个不同按钮」的原因。动效只有两种含义：
      **横向**（同级之间切换，新的从旧的离开的方向进来）和**掀开**（面板从它所在的那条边
      来，关的时候你知道它去哪了）。分屏开关不再只在两个 tab 上出现。
- [x] **V3 右栏可拖拽分 pane。** 竖着堆的 pane，每个自带 tab 条。57 条测试全部只断言同一个
      不变量：**每个 tab 恰好在一个 pane 里，且每个 tab 都在某处**——这一条同时买到了「拖不
      丢」、「存坏的布局会自愈」和「每个 pane 有自己的状态」（pane 的状态**就是**它那个 tab 的）。
      存在 localStorage，key 按视口档位分（宽和高都算，因为 pane 是竖着堆的），永远不上服务器。
      没有鼠标也能用：每个 pane 一个菜单加 `Alt`+方向键。手机上这个功能是**不存在**而不是模拟。
- [x] **V4 README 重写。** 顺带查出六条当时写着但并不成立的话。
- [x] **V5 看板生成器。** 只读链接不该只用来看 agent；**十种以上不同用途、可自定义**，
      从头构思。**十九个起手式，按「谁在看这块屏」分组**而不是按数据来源分组——那是这件事
      唯一的设计决定，按来源分只会得到十四个长得一样的表格。二十一种组件，看板存在链接里
      （所以以后改目录不会改掉别人已经贴到电视上的那面墙），范围可以限定到一个项目或一个
      会话。写权限**没有做**，`docs/writable-links.md` 是提案。
- [x] **V6 一行命令安装。** 直接从 GitHub 拉、macOS/Linux 都行、tmux 缺失或过旧时提示并
      带自动安装流程、**默认推荐装成系统服务**、外加一个管理命令。装完之后不管用哪种方式跑的，
      都是同一条 `vibepanel service status|start|stop|restart|logs|token|upgrade|uninstall`。
      账号可以在脚本里建（`--username` / `--password-file`；**故意没有** `--password <值>`，
      那是把密码写进 shell history 和 `ps`），也可以照旧在 web 控制台建。
- [x] **V7 产品里不要讲道理。** 「把他妈类似这种描述从产品里删掉 这些话是说给我听的
      没必要写进产品」——字典里最长的 11 条全是论证，已经改成说完就停。

## 第五批（2026-08-27，发布之后）

- [x] **W1 一行安装加 GitHub 镜像。** `--mirror`，默认 `github.muran.tech`。镜像按 IP 授权，
      没授权时返回 401 加一段带验证链接的文字，而这段文字正是唯一有用的东西——`curl -f`
      会把它丢掉，管道里没人可问，写成 `curl -f ... | sh` 屏幕上一片空白。所以：单独发一次
      不带 `-f` 的请求把原文打出来，管道里打完链接后以 **3** 退出（自己的状态码，外层能把
      「去点个链接」和「下载坏了」分开），有终端就等你回车重试。**opt-in，永不自动**：镜像
      下发布包和校验它的 SHA256SUMS 来自同一台机器，校验就不再说明镜像本身的任何事情。
      只有 GitHub 自己的域名会被改道。7 个变异，2 个活下来（假镜像按 basename 服务，分不出
      「只改道 GitHub」和「全都改道」；`--mirror` 吞掉后一个参数没人测），都已修。
- [x] **W2 发布 v0.1.0。** 仓库此前一个 tag 一个 release 都没有，所以那条一行安装命令对
      任何人都是坏的——问 GitHub 要最新 release，拿到空数组。已打 tag、建 release、上传三个
      归档和 SHA256SUMS，并用**真实镜像 + 真实 release** 完整跑通了 `--mirror` 和直连两条路。
- [x] **W3 面板里不要讲道理（第三次）。** 十四条字典字符串是在**论证**，不是在说事。
      `i18n.prose.test.ts` 用长度上限钉住（中文 38、英文 105，占位符不计）。上限从 46 收到 38
      才让变异变红——46 个汉字足够写完一整段道理。
- [x] **W4 分享链接的服务端管理。** remark / lock / 换模版，改动实时到达所有已打开的 viewer；
      看每个链接连了几个 client；admin 端和 view 端都能配置布局（view 端的布局是本地的，
      不新增任何写路由——红线 8）。*(子任务进行中)*
- [x] **W5 整个 app 的 chrome 统一。** 12 个文件、22 种手写变体、40 个控件收进
      `.vp-control` / `.vp-tab` / `.vp-segmented`,并加了 `chrome-vocabulary.test.ts` 守着。
      值得记的 bug 是触摸下限:`.vp-tap` 的 44px `min-width` 定义在样式表几百行之前,
      `.vp-control` 的 `min-width` 也是一个类,**优先级打平、由源码顺序决定**——静悄悄地
      判给了 28px。是 render-check 量出来的(三个手机控件 28×44)。

## 第六批（2026-08-28）

- [x] **W6 安装脚本会问语言，可以全程中文。** 分界线不是「翻多少」而是：**参与决策的句子翻，
      已经发生的事的记录可以不翻**。所以每个提问、动手前的计划、每条「告诉你下一步做什么」的
      错误、以及摘要连同它下面的 note——**作为一个整体**翻，因为「中文的问题 + 英文的后果」
      正是要避免的失败。剩九行英文是 verb+path 的执行日志。优先级：`--lang` > `LC_ALL` /
      `LC_MESSAGES` / `LANG` 里**第一个被设置的**（不认识就英文，**不往下穿**——`LC_ALL=C`
      旁边挂 `LANG=zh_CN` 是有人明确要 C）> 提问。提问在**横幅之前**：答完三个英文问题再选
      语言，已经晚了。`curl | sh` 时引导脚本自己就是 stdin，没人可问，所以只有内层问；
      `--password-stdin` 时它退开，否则会读走密码第一行并回显进日志。缺 key 打标记不打空行。
      15 个变异，第一轮活 2 个，都是把检查改严修掉的。
- [x] **W7 右栏两个 tab。** `PANEL_TABS = ['files', 'notes']`。监视器和用量不再是 tab——
      **tab 是一个去处，而这两个都不是**，它们是你读别的东西时想用余光扫到的，所以两个 tab
      共用同一个下半段（token 用量在上、机器监控在下，顺序尺寸都一样）。git 收进文件树表头
      一行（分支 · 领先/落后 · 未提交 · 冲突），点一下展开。todo 的 UI 整个删掉，**路由留着**
      ——墙上的看板真的在调它们（`todos` 组件、`todoPercent` 指标、四个预设），删了会改掉
      别人已经贴出去的墙。三态：紧凑 → 占满侧栏 → 占满窗口，**每层替换下一层**，Esc 每次退
      一级，都不持久化。29 个变异全红。
      - **时间 = 读数的新鲜度和窗口**，不是累计耗时：面板只有 `stateChangedAt`，没有任何东西
        能加总成「花了多少时间」，而且 agent 无人值守跑，一个开了一夜的会话会压垮这个数。
      - **字数 = 输出 token**，标签写「输出」。token 不是字，中文尤其不是，换算需要面板没有的
        分词器；输入/缓存读/缓存写三样都是「问」的成本，只有输出是产出。
      - **只读看板上的 GitHub 链接只在 `names` 档、只对限定到项目的链接、只发 owner+name，
        永不发 URL。** `github.com/acme-holdings/payroll` 暴露客户的精确度不比项目路径低，
        而 `counts` 档正是「屏幕前有摄像头」时选的那一档。它的属性测试还查出一个真洞：
        `..` 能通过字符类，`github.com/../payroll` 解析到的是另一个仓库。
- [x] **W8 终端右边 40px 黑边 → 26px，滚动条 14px → 8px。** 实测 1400px 宽度下从右往左：
      文字到 1071、滚动条 1075–1089、content box 到 1089、`padding-right:14px` 到 1103、
      外层 `p-2` 到 1111。**滚动条画在 content box 里面**，所以那 14px padding 没有把文字和
      滚动条隔开，它是加在**滚动条右边**的 14px 纯黑——这就是「进度条旁边还有黑边」。真正让
      文字不被压住的是网格取整余数，那是运气，某些宽度下是 0。现在预留和被预留的是同一个数
      `--vp-scrollbar`。终端拿回 14px 宽度（798 → 812）。
- [x] **W9 挪动布局不自适应 + 意义不明的接管按钮。** 同一行代码。client id 存在
      `sessionStorage`，**扛得过刷新、扛不过关标签页**，所以「明天再打开」永远是陌生人，而
      陌生人进入一个「控制者已离开」的会话时拿不到网格：终端按昨天的网格缩放到角落（两边留黑），
      改布局只重新缩放而不重排，唯一出路是一个「向没有人接管」的按钮。`Unsubscribe` 的注释
      八个月前就把这写成了已知代价，它读起来像边缘情况，实际是主路径。
      **第一版改成「只有你一个人就给你」，方向错了**：桌面关标签页、手机一分钟后打开成了唯一
      观看者，把 147 列压成 46 列——没人可保护，伤害照样发生。**「是不是只有一个人」不是区分
      「在刷新」和「已经走了」的属性，时间才是。** 现在是两分钟宽限期，三个变异全红。
- [x] **W10 发布 v0.1.0 + 一行安装加 GitHub 镜像。** 见第五批 W1/W2。

### 还欠着的

- [ ] **X1 `bottom: typing into a bottom terminal produced no output` 还会间歇失败。**
      今天修掉了其中一半——`vibepanelScreen` 在没有焦点时**静悄悄读第一个终端**，于是往底部
      终端打的字被拿去和主终端比对。那一半是真 bug（主终端此前根本没有 `data-session-id`，
      这就是所有调用方只能问「焦点在哪」的原因）。但子任务在**有负载的机器上**仍然复现了两次，
      而我在空闲机器上跑四次都是 0 FAIL——**剩下那半大概率是 12 秒等待窗口在负载下不够**。
      修法不是把 sleep 调长，而是**先等 shell 打出提示符再打字，并且把等待时长报出来**：
      如果它总是 0ms，说明又猜错了，得继续找。
- [ ] **X2 面板里没有任何东西覆盖 VNC 标签页的浏览器检查。** 需要 `x11vnc -localhost` 配
      `Xvfb`，或者一个只说握手再发一个 `Raw` 矩形的假服务端。
- [x] **X3 GitHub 那半的缓存。** `internal/git/warm.go`：stale-while-revalidate，工作树 90 秒、
      GitHub 5 分钟，多个 viewer 共用，没有定时器，而且**轮询永远不执行任何东西**——
      `TestTheFirstPollDoesNotWaitForGit` 断言第一次轮询直接返回 `readable: false`。

## 第七批（2026-08-28，把 README 对着代码读了一遍之后）

- [x] **X4 通知开关承诺了一件不会发生的事。** `notifyOnWaiting` 写完了、注释齐全、两个 README
      都写了它，**没有任何地方调用它**——设置里那个开关要了通知权限、存了个布尔值，然后什么
      都不会发生。不是编译错误，不是 lint 错误，两次 review 都过了。**发现它的方式是拿 README
      对着代码逐条读。**
- [x] **X5 明文 HTTP 下所有复制都静默失败。** `navigator.clipboard?.writeText(t).catch(f)`
      —— 可选链短路整条链，所以没有 clipboard 对象时 `.catch` 根本不会被调用。这一行在五个
      文件里写了五遍，四遍是错的,其中两处是**「复制这个令牌，你只会看到这一次」**的按钮。
      收成一个模块，守卫是扫源码（这个 bug 不在函数做了什么，而在它是怎么被调用的）。
- [x] **X6 `wired.test.ts`：每个模块都得被什么东西引用到。** 上面两条都是「代码在、但没接上」
      的同一种病，而且都不是编译错误。这个测试一上来就又查出四个没人调用的导出，其中两个是
      当天刚合并进来的。`BigMeter` 那条是**正确的报告**：它只在自己文件里被用，那不是死代码，
      那是不该导出——去掉 `export` 才是修法，删掉函数不是。
- [x] **X7 `VncView.tsx` 删掉。** tab 退役时组件留在了原地，什么都没引用它。

### 还欠着

- [ ] **X1 `bottom terminal` 那个间歇失败。** 后半段的原因找到了：断言要数两次
      `BOTTOM_TERMINAL_OK`，而**工作树路径长的时候 shell 提示符把回显的命令折了行**，
      字符串被切成两半，只剩一次匹配。修法是匹配前先把缓冲区的行接起来，不是调长等待。
- [x] **X2 VNC 服务端那半也收掉了。**「vnc服务端收口」——两条路里选了第二条。删掉的是
      `internal/vnc`（policy / proxy / RFB 握手）、`internal/httpapi/vnc.go`、
      `internal/store/vnc.go`、`--vnc` 和 `--vnc-allow`、设置页那一节、`novnc` 依赖，
      一共约 2900 行。**`RETIRED_TABS` 里的 `'vnc'` 留着**：那是别人浏览器里现在就存着的
      字符串，布局修复路径靠它。迁移**没有删 v14**——迁移是按位置编号的，删一条等于把后面
      每一条都改号——而是新加了 v17 `DROP TABLE vnc_targets`：那张表的 `password` 列是**明文**
      存的，功能都没了还把别人的密码留在库里，那不叫收口。
- [ ] **X8 `docs/images/panel-dark.png` 落后一个版本**，画的还是五个 tab 的侧栏。
      重新截图是 `web/scripts/shots.mjs` 配一个新构建。
